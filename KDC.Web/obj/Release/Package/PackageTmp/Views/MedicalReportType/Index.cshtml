@model IEnumerable<KDC.DL.Models.MS_MedicalReportType>

@{
    ViewBag.Title = "Index";
}

<section class="content-header">
    <h1>
        Medical Report Types
    </h1>
    <ol class="breadcrumb">
        <li class="list"><a href="@Url.Action("Create", "MedicalReportType")"><i class="fa fa-plus-circle"></i> Add </a></li>
    </ol>
</section>

<section class="content">
    <div class="box">
        <div class="box-body">
            <div class="table-responsive">
                <table id="example" class="table table-bordered table-condensed">
                    <thead>
                        <tr>
                            <th>@Html.DisplayName("Report Type Name")</th>
                            <th>Action</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ReportTypeName)
                                </td>
                                <td>
                                    @Html.ActionLink(" ", "Edit", new { id = item.ReportTypeId }, new { @class = "fa fa-pencil text-primary", title = "Edit" }) |
                                    <a href="@Url.Action("Delete", "MedicalReportType", new { @id = item.ReportTypeId})" onclick='return confirmDelete();'><span class="fa fa-trash text-danger"></span></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
<script>
	function confirmDelete() {
		return confirm('Are you sure you want to delete this?');
	}
</script>