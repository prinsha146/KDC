@model IEnumerable<KDC.DL.Models.MS_JobProfession>

@{
    ViewBag.Title = "Index";
}

<section class="content-header">
    <h1>
        Job Profession List
    </h1>
    <ol class="breadcrumb">
        <li class="list"><a href="@Url.Action("Create", "JobProfession")"><i class="fa fa-plus-circle"></i> Add </a></li>
    </ol>
</section>

<section class="content">
    <div class="box">
        <div class="box-body">
            <div class="table-responsive">
                <table id="example" class="table table-bordered table-condensed">
                    <thead>
                        <tr>
                            <th>@Html.DisplayName("Job Profession Name")</th>
                            <th>Action</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.JobProfessionName)
                                </td>

                                @if (item.Active == true)
                                {
                                    <td><span class="label label-success">Active</span></td>
                                }
                                else
                                {
                                    <td><span class="label label-danger">Inactive</span></td>
                                }
                                <td>
                                    @Html.ActionLink(" ", "Edit", new { id = item.JobProfessionId }, new { @class = "fa fa-pencil text-primary", title = "Edit" }) |
                                    @Html.ActionLink(" ", "Details", new { id = item.JobProfessionId }, new { @class = "fa fa-list text-success", title = "Details" }) |
                                    <a href="@Url.Action("Delete", "JobProfession", new { @id = item.JobProfessionId})" onclick='return confirmDelete();'><span class="fa fa-trash text-danger"></span></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
<script>
	function confirmDelete() {
		return confirm('Are you sure you want to delete this?');
	}
</script>